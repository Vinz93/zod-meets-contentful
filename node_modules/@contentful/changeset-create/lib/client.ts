import { contentTypesClient } from './content-type'
import { editorInterfaceClient } from './editor-interface'

export const CreateChangeset = {
  contentTypes: contentTypesClient,
  editorInterface: editorInterfaceClient,
}
