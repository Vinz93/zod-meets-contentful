"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createFieldHash = void 0;
const createFieldHash = (fieldIdMapForContentType) => (field, _index, position) => {
    // 'left' means that the field comes from the target environment,
    // 'right' comes from the source environment
    const idToCompare = (position === 'right' && fieldIdMapForContentType?.source.get(field.id)) ||
        (position === 'left' && fieldIdMapForContentType?.target.get(field.id)) ||
        field.id;
    return `${idToCompare}-${field.type}`;
};
exports.createFieldHash = createFieldHash;
